---
name: echo-skill
description: The user's own echo skill, which shadows the bundled one. Use when a test wants precedence.
metadata:
  category: testing
---

# Echo (user)

The user's version wins over the bundled one.
